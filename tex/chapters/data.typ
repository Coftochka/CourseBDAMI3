#import "../state.typ": bib_state
#context bib_state.get()

= Работа с данными <sec-data>

== Данные

В работе используются дневные данные о свечах всех валидных акций Московской биржи. 
Свечи базово включают 5 основных параметров помимо timestamp:

#align(center, table(
  columns: 5,
  [*open*], [*high*], [*low*], [*close*], [*volume*],
))

где *open* — цена открытия свечи, *high* — максимальная цена свечи, *low* — минимальная цена свечи, *close* — цена закрытия свечи, *volume* — объём торгов.

Эти данные были собраны за период с начала существования акции на бирже вплоть до 2026 года.

#figure(
  image("sber_candles.png", width: 100%),
  caption: [Дневные свечи акции SBER],
)

== Предобработка данных и создание признаков

Перед добавлением признаков из данных были удалены пропуски, а также исключены все акции, у которых было менее 252 торговых дней. Таким образом из 260 акций осталось 236.

Далее для каждого временного ряда были добавлены следующие признаки:

#align(center, table(
  columns: 2,
  [*sma5*], [*sma20*],
  [*ema12*], [*ema26*],
  [*close_sma20*], [*macd*],
  [*macd_signal*], [*macd_hist*],
  [*rsi14*], [*bb_pct*],
  [*bb_bw*], [*atr14*],
  [*obv*],
))

где признаки вычисляются по формулам:

- $"sma5"_t = 1/5 sum_(i=0)^4 "close"_(t-i)$ - средняя цена за 5 дней

- $"sma20"_t = 1/20 sum_(i=0)^19 "close"_(t-i)$ - средняя цена за 20 дней

- $"ema12"_t = alpha_12 "close"_t + (1 - alpha_12) "ema12"_(t-1), alpha_12 = 2/(12+1)$ - экспоненциальная средняя цена за 12 дней

- $"ema26"_t = alpha_26 "close"_t + (1 - alpha_26) "ema26"_(t-1), alpha_26 = 2/(26+1)$ - экспоненциальная средняя цена за 26 дней

- $"close_sma20"_t = "close"_t / "sma20"_t$ - отношение цены к средней цене за 20 дней

- $"macd"_t = "ema12"_t - "ema26"_t$ - разница между экспоненциальными средними за 12 и 26 дней 

- $"macd_signal"_t = "ema9"("macd"_t)$ - экспоненциальная скользящая средняя за 9 дней от $"macd"_t$

- $"macd_hist"_t = "macd"_t - "macd_signal"_t$ - разница между $"macd"_t$ и $"macd_signal"_t$

- $"rsi14"_t = 100 - 100/(1 + "RS"_t), "RS"_t = "avgGain14"_t / "avgLoss14"_t$ - индекс относительной силы за 14 дней

- $"sigma20"_t = "std"("close"_(t-19), dots, "close"_t)$ - стандартное отклонение цены за 20 дней

- $"bb_upper"_t = "sma20"_t + 2 times "sigma20"_t, "bb_lower"_t = "sma20"_t - 2 times "sigma20"_t$ - верхняя и нижняя границы полос Боллинджера

- $"bb_pct"_t = ("close"_t - "bb_lower"_t) / ("bb_upper"_t - "bb_lower"_t)$ - относительное положение цены внутри полос Боллинджера

- $"bb_bw"_t = ("bb_upper"_t - "bb_lower"_t) / "sma20"_t$ - ширина полос Боллинджера

- $"TR"_t = "max"("high"_t - "low"_t, "abs"("high"_t - "close"_(t-1)), "abs"("low"_t - "close"_(t-1)))$ - истинный диапазон

- $"atr14"_t = "ema14"("TR"_t)$ - экспоненциальная средняя истинного диапазона за 14 дней

- $"obv"_t = "obv"_(t-1) + "sign"("close"_t - "close"_(t-1)) times "volume"_t$ — накопленный баланс объёма торгов

где $"close"_t$, $"high"_t$, $"low"_t$, $"volume"_t$ — значения цены закрытия, максимума, минимума и объёма в день $t$ соответственно.

Таким образом, всего получается 18 признаков, на которых обучаются модели.

#align(center, table(
  columns: 2,
  [*open*], [*high*], [*low*],
  [*close*], [*volume*], [*sma5*],
  [*sma20*], [*ema12*], [*ema26*],
  [*close_sma20*], [*macd*], [*macd_signal*],
  [*macd_hist*], [*rsi14*], [*bb_pct*],
  [*bb_bw*], [*atr14*], [*obv*],
))


Все данные были разделены на три непересекающихся временных выборки: тренировочную, валидационную и тестовую. Разбиение выполнено по дате, а не случайно, чтобы исключить утечку данных из будущего в прошлое.

Поскольку часть акций появилась на MOEX лишь в 2014–2015 годах, тренировочная выборка охватывает наибольший временной промежуток.

#align(center, table(
  columns: 3,
  stroke: 0.5pt,
  inset: 6pt,
  align: center,
  [*Выборка*], [*Период*],         [*Доля*],
  [Train],      [2000 — 2022],      [~5/7],
  [Validation], [2023 — 2024],      [~1/7],
  [Test],       [2025 — март 2026], [~1/7],
)).


