#import "../state.typ": bib_state
#context bib_state.get()

= Кластеризация эмбеддингов

Кластеризация представляет собой процесс группировки объектов (в нашем случае — эмбеддингов) на основе их схожести.

Для кластеризации были использованы следующие методы:

- KMeans — метод кластеризации на основе $k$-средних.
- HDBSCAN — иерархический плотностной метод кластеризации.

== KMeans

KMeans — метод, который принимает на вход $k$ — количество кластеров и определяет центроиды кластеров.

На каждой итерации алгоритм вычисляет расстояние от каждого объекта до каждого центроида и относит объект к ближайшему кластеру, после чего пересчитывает центроид как среднее арифметическое координат всех объектов кластера.
Этот процесс повторяется до тех пор, пока центроиды не стабилизируются.

Подбор оптимального числа кластеров проводился по метрике *silhouette* (коэффициент силуэта).

Пусть $d(i, j)$ — расстояние между объектами $i$ и $j$ в пространстве эмбеддингов. Обозначим через $C(i)$ кластер, которому принадлежит объект $i$.

*Внутрикластерная средняя дистанция:*
$ a(i) = 1/(|C(i)| - 1) sum_(j in C(i), j != i) d(i, j) $

*Минимальная средняя дистанция до «чужого» кластера:*
$ b(i) = min_(C != C(i)) 1/|C| sum_(j in C) d(i, j) $

*Силуэт точки $i$:*
$ s(i) = (b(i) - a(i)) / max(a(i), b(i)) $

*Средний силуэт по выборке из $n$ объектов:*
$ "Silhouette" = 1/n sum_(i=1)^n s(i) $

Чем ближе значение к $1$, тем лучше разделимость кластеров; значения около $0$ указывают на перекрытие кластеров.

Поскольку соседние окна с большим перекрытием с высокой вероятностью принадлежат одному кластеру, окна формировались не подряд с шагом 1 день, а с шагом 5 дней. Это позволяет избежать избыточного дублирования информации и при этом незначительно сокращает общий объём данных.


=== Визуализация и анализ кластеров

==== Метрики анализа кластеров

Для анализа кластеров были вычислены следующие метрики:

- *mean return* — средняя доходность по кластеру, позволяющая судить о прибыльности соответствующего рыночного режима.

- *std return* — стандартное отклонение доходности по кластеру, характеризующее рискованность режима.

- *sharpe (proxy)* — прокси-коэффициент Шарпа по кластеру, описывающий соотношение доходности и риска.

- *kurtosis* — эксцесс распределения доходности, отражающий «тяжесть» хвостов и склонность к экстремальным значениям.

- *skewness* — асимметрия распределения доходности, показывающая, в какую сторону смещён хвост распределения.

==== TS2Vec

#figure(
   image("cluster_pca2d_ts2vec.png", width: 120%),
   caption: [Визуализация кластеров TS2Vec],
)


При использовании TS2Vec для построения эмбеддингов наилучший результат получился при числе кластеров $k = 5$ и коэффициенте силуэта $"silhouette" = 0.11$.





#figure(
   image("cl_dist_ts2vec.png", width: 120%),
   caption: [Распределение кластеров TS2Vec],
)


Как видно из графика, кластеры содержат примерно одинаковое количество объектов.
Это говорит о том, что кластеризация прошла успешно и не возникло вырожденных кластеров с критически малым числом элементов, на которых было бы невозможно обучить модель.
Сбалансированность распределения свидетельствует о том, что алгоритм K-Means нашёл устойчивое разбиение пространства эмбеддингов: ни один кластер не доминирует над остальными и не поглощает большую часть обучающей выборки.
Это важное свойство с практической точки зрения — каждый кластер представляет собой отдельный режим рынка с достаточным количеством примеров для надёжного обучения специализированных моделей прогнозирования.

#figure(
   image("cl_an_ts2vec.png", width: 100%),
   caption: [Метрики анализа кластеров TS2Vec],
)

*Кластер C0 — нейтральный (боковое движение).*
Все метрики данного кластера располагаются вблизи нулевой отметки на радарном графике.
Средняя доходность и коэффициент Шарпа близки к нулю, волатильность умеренная, асимметрия и эксцесс не выражены.
Такие окна соответствуют периодам рыночной неопределённости, когда цена движется в узком диапазоне без устойчивого тренда.
С точки зрения торговой стратегии данный кластер наименее информативен и характеризует «боковик».

*Кластер C1 — устойчивый рост (бычий тренд).*
Кластер демонстрирует наибольшую среднюю доходность и наиболее высокий прокси-коэффициент Шарпа среди всех кластеров.
Волатильность при этом остаётся умеренной, что обеспечивает благоприятное соотношение доходности к риску.
Положительная асимметрия распределения указывает на то, что редкие крупные прибыльные движения присутствуют чаще, чем убыточные выбросы.
Данный кластер соответствует устойчивым бычьим периодам рынка, на которых длинные позиции наиболее эффективны.

*Кластер C2 — высокая волатильность (нестабильный рынок).*
Отличительная черта кластера — значительно завышенное стандартное отклонение доходности и высокий эксцесс распределения.
Это свидетельствует о наличии резких ценовых движений в обоих направлениях: как сильных ростов, так и глубоких падений.
Средняя доходность при этом близка к нулю, а коэффициент Шарпа низкий, что делает прогнозирование направления движения крайне затруднительным.
Подобные режимы характерны для периодов выхода важных макроэкономических новостей или корпоративных событий.

*Кластер C3 — снижение (медвежий тренд).*
Данный кластер характеризуется отрицательной средней доходностью и отрицательным значением прокси-коэффициента Шарпа, что однозначно указывает на медвежий режим рынка.
Волатильность повышена, распределение имеет выраженную отрицательную асимметрию — убыточные движения в среднем превышают прибыльные.
Окна этого кластера соответствуют периодам устойчивого снижения котировок, при которых короткие позиции или выход из рынка являются предпочтительной стратегией.

*Кластер C4 — аномальный (экстремальные движения).*
Кластер выделяется высоким положительным скосом (skewness) при умеренных остальных показателях.
Это указывает на нечастые, но очень крупные положительные выбросы доходности — так называемые «gap up» или ударные дни роста.
Несмотря на умеренную среднюю доходность, хвост распределения значительно вытянут вправо.
Такой профиль характерен для периодов резкого восстановления после коррекции или реакции на позитивные корпоративные события.




==== Handmade

#figure(
   image("cluster_pca2d_handmade.png", width: 120%),
   caption: [Визуализация кластеров Handmade],
)

При использовании ручных (handmade) признаков для построения эмбеддингов наилучший результат получился при числе кластеров $k = 4$ и коэффициенте силуэта $"silhouette" = 0.16$.


#figure(
   image("cl_dist_handmade.png", width: 120%),
   caption: [Распределение кластеров Handmade],
)

Распределение окон по кластерам в целом умеренно сбалансированно: кластеры C0, C1 и C2 содержат сопоставимое число объектов (28–34 тысячи на обучающей выборке), тогда как кластер C3 заметно меньше — около 19 тысяч окон.
Аналогичная пропорция сохраняется на валидационной и тестовой выборках, что подтверждает устойчивость разбиения.
Относительно меньший размер C3 обусловлен редкостью соответствующего рыночного режима, однако его объём остаётся достаточным для обучения отдельной модели.

#figure(
   image("cl_an_handmade.png", width: 100%),
   caption: [Метрики анализа кластеров Handmade],
)

*Кластер C0 — нейтральный (боковое движение).*
На радарном графике все метрики данного кластера сосредоточены вблизи нулевой отметки и не имеют выраженных пиков.
Средняя доходность, стандартное отклонение, коэффициент Шарпа, асимметрия и эксцесс — все принимают умеренные значения.
Данный кластер соответствует периодам низкой активности рынка без выраженного направления движения.

*Кластер C1 — устойчивый рост (бычий тренд).*
Кластер демонстрирует наибольшую среднюю доходность и наибольший прокси-коэффициент Шарпа среди всех четырёх кластеров, при умеренной волатильности.
Сочетание высокой средней доходности и относительно небольшого стандартного отклонения формирует максимально благоприятный профиль риск/доходность.
Данный кластер соответствует устойчивым периодам роста, на которых стратегия удержания длинной позиции наиболее эффективна.

*Кластер C2 — нисходящий рынок с отрицательной асимметрией.*
Отличительная черта кластера — выраженный отрицательный эксцесс (kurtosis) и отрицательная асимметрия (skewness), при этом средняя доходность близка к нулю или слабо отрицательна.
Отрицательный эксцесс указывает на «плосковершинное» распределение с укороченными хвостами — доходности менее склонны к экстремальным значениям, чем нормальное распределение.
Отрицательный скос свидетельствует о том, что редкие крупные убытки всё же присутствуют, тогда как прибыльные движения более равномерны.
Подобный профиль характерен для вялотекущего снижения без резких скачков.

*Кластер C3 — высокая волатильность с падением (кризисный режим).*
Кластер выделяется экстремально высоким стандартным отклонением доходности, при этом средняя доходность является отрицательной, а коэффициент Шарпа — наихудшим среди всех кластеров.
Одновременно наблюдается выраженная положительная асимметрия: рынок резко падает, однако периодически демонстрирует сильные отскоки вверх.
Данный режим наиболее сложен для прогнозирования ввиду высокой неопределённости и характерен для острых рыночных кризисов или панических распродаж.


==== Сравнение TS2Vec и Handmade

Сопоставляя результаты кластеризации на основе handmade-признаков и TS2Vec-эмбеддингов, можно отметить, что оба подхода выделяют схожие рыночные режимы: нейтральный, бычий, медвежий и кризисный, — что подтверждает содержательность полученного разбиения.

Из полученных результатов можно сделать вывод, что кластеры действительно отражают различные режимы рынка, а гипотеза о целесообразности использования разных моделей для разных режимов подтверждается.

Примечательно, что при использовании handmade-признаков коэффициент силуэта оказался выше ($0.16$ против $0.11$), что свидетельствует о более чётком разделении кластеров в этом пространстве.
Это объясняется тем, что вручную спроектированные статистики — средняя доходность, волатильность, асимметрия — непосредственно описывают характер ценового движения внутри окна и потому хорошо дифференцируют рыночные режимы.
TS2Vec, в свою очередь, строит более богатые контекстные представления, однако его эмбеддинги несут информацию о тонких временны́х зависимостях, которые K-Means труднее разделяет в метрике евклидова расстояния.


== HDBSCAN

HDBSCAN — принимает на вход минимальный размер кластера и масштабирует данные перед кластеризацией.

Метод выбирает плотные области и объединяет их в кластеры, при этом не учитывается количество кластеров, что, с одной стороны помогает избежать перебора количества кластеров, но с другой стороны выделяет большое количество маленьких кластеров, которые не годятся для обучения и 1 - 2 больших кластеров, которые уже можно использовать.

Проблемма 1 - 2 больших кластеров в том, что они не выделяют сильной информации в данных и, как правило просто зависят от доходдности, а вот маленькие кластеры выделяют индивидуальную структуру окон, но зачастую содержат порядка 1000 объектов.






